# pwn.notes — MkDocs build dependencies
# Pin minor versions so builds stay reproducible; bump intentionally.
mkdocs-material>=9.5,<10
mkdocs>=1.6,<2
pymdown-extensions>=10.7
# Twemoji SVG icons used across the notes
mkdocs-material-extensions>=1.3
