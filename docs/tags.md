---
hide:
  - navigation
---

# :material-tag-multiple: Tags

Every note is tagged so you can pivot across topics the way a real engagement does — jump from a `windows` privesc note to `windows` AD attacks, or follow the `cloud` thread from an SSRF to an IAM privesc.

!!! tip "Also try full-text search"
    Press <kbd>S</kbd> or <kbd>/</kbd> and type anything — a tool name (`ligolo`), a technique (`kerberoast`), or an endpoint (`169.254.169.254`). Search indexes headings and code, not just prose.

<!-- material/tags -->
